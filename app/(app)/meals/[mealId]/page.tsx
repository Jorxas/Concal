type MealDetailPageProps = {
  params: Promise<{ mealId: string }>;
};

/** Meal detail (protected). UI later. */
export default async function MealDetailPage(_props: MealDetailPageProps) {
  return null;
}
